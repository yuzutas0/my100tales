<div class="layout__tale__edit">
  <p><%= render 'breadcrumb_3_tale', forth_page: Constants::EDIT_TALE_PATH_NAME %></p>

  <%= form_for @tale do |f| %>
    <%= render 'form', f: f %>
  <% end %>

  <%= link_to 'Delete Tale', @tale, method: :delete, data: { confirm: 'Are you sure?' }, class: 'text-danger' %>
</div>
