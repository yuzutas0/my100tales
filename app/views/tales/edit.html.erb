<% @page_title = t('views.path.name.tale.edit') %>
<div class="layout__tale__edit">
  <p><%= render 'breadcrumb_3_tale', forth_page: @page_title, tale: @tale %></p>

  <%= form_for @tale do |f| %>
    <%= render 'form', f: f, tags: @tags %>
  <% end %>
</div>
