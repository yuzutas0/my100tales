<div class="layout__tale__new">
  <p><%= render 'layouts/breadcrumb_2', third_page: Constants::NEW_TALE_PATH_NAME %></p>
  <h2><%= Constants::NEW_TALE_PATH_NAME %></h2>

  <%= form_for(@tale, url: create_tale_path) do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
