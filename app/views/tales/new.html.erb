<div class="layout__tale__new">
  <p><%= render 'layouts/breadcrumb_tale_2', third_page: Constants::NEW_TALE_PATH_NAME %></p>

  <%= form_for(@tale, url: create_tale_path) do |f| %>
    <%= render 'form', f: f, tags: @tags %>
  <% end %>
</div>
