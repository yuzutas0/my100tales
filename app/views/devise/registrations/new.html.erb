<div class="layout__devise">
  <h2>Sign up</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'form' }) do |f| %>
    <%= devise_error_messages! %>

    <div class="form__field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, class: 'form__field__text' %>
    </div>

    <div class="form__field">
      <%= f.label :name %><br />
      <%= f.text_field :name, autofocus: true, class: 'form__field__text' %>
    </div>

    <div class="form__field">
      <%= f.label :password %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: 'off', class: 'form__field__text' %>
    </div>

    <div class="form__field">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: 'off', class: 'form__field__text' %>
    </div>

    <div><%= f.submit 'Sign up', class: 'button' %></div>
  <% end %>

  <%= render 'devise/shared/links' %>
</div>
