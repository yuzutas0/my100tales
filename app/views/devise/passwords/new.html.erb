<% @page_title = Constants::PASSWORD_PATH_NAME %>
<div class="layout__devise">
  <h2>Forgot your password?</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'module__form' }) do |f| %>
    <%= devise_error_messages! %>

    <div class="module__form__field">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, class: 'module__form__field__text' %>
    </div>

    <div><%= f.submit 'Send me reset password instructions', class: 'module__button' %></div>
  <% end %>

  <%= render 'devise/shared/links' %>
</div>
